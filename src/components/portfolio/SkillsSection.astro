---
interface Skill {
  name: string;
  level?: number; // 1-5
  category?: string;
}

interface Props {
  skills?: Skill[];
}

const { skills = [] } = Astro.props;

const grouped = skills.reduce<Record<string, Skill[]>>((acc, skill) => {
  const cat = skill.category ?? "General";
  if (!acc[cat]) acc[cat] = [];
  acc[cat].push(skill);
  return acc;
}, {});

const categories = Object.entries(grouped);
---

<section>
  <h2 class="text-2xl font-bold text-th-text">
    Skills
  </h2>
  <div class="mt-1 h-1 w-12 rounded-full bg-gradient-to-r from-primary-500 to-accent-500"></div>

  {categories.length > 0 ? (
    <div class="mt-8 space-y-8">
      {categories.map(([category, items]) => (
        <div>
          <h3 class="mb-4 text-sm font-semibold uppercase tracking-widest text-th-text-faint">
            {category}
          </h3>
          <div class="grid grid-cols-2 gap-3 sm:grid-cols-3 lg:grid-cols-4">
            {items.map((skill) => (
              <div class="group rounded-xl border border-th-border bg-th-bg-card p-4 transition-all hover:border-primary-500/30 hover:bg-th-bg-subtle">
                <p class="text-sm font-medium text-th-text-strong">
                  {skill.name}
                </p>
                {skill.level && (
                  <div class="mt-2 flex gap-1">
                    {Array.from({ length: 5 }).map((_, i) => (
                      <span
                        class={`h-1 flex-1 rounded-full ${
                          i < skill.level!
                            ? "bg-primary-500"
                            : "bg-th-skeleton"
                        }`}
                      />
                    ))}
                  </div>
                )}
              </div>
            ))}
          </div>
        </div>
      ))}
    </div>
  ) : (
    <div class="mt-8 grid grid-cols-2 gap-3 sm:grid-cols-3 lg:grid-cols-4">
      {Array.from({ length: 8 }).map(() => (
        <div class="rounded-xl border border-th-border bg-th-bg-card p-4">
          <div class="h-4 w-20 rounded bg-th-skeleton"></div>
          <div class="mt-2 flex gap-1">
            {Array.from({ length: 5 }).map(() => (
              <span class="h-1 flex-1 rounded-full bg-th-skeleton" />
            ))}
          </div>
        </div>
      ))}
    </div>
  )}
</section>
